<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/summary_background"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="5dip">

    <!-- Thumbnail image -->
    <LinearLayout
        android:id="@+id/summary_thumbnail_ll"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_marginEnd="5dip"
        android:padding="3dip">

        <ImageView
            android:id="@+id/summary_thumbnail_image_view"
            android:layout_width="50dip"
            android:layout_height="50dip" />

    </LinearLayout>

    <!-- Question prompt -->
    <TextView
        android:id="@+id/summary_prompt_text_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/summary_thumbnail_ll"
        android:layout_toEndOf="@+id/summary_thumbnail_ll"
        android:text="Which flower is this?"
        android:textColor="#d70d2b"
        android:textSize="20sp"
        android:textStyle="bold"
        android:typeface="monospace" />

    <!-- Users response -->
    <TextView
        android:id="@+id/summary_response_text_view"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/summary_prompt_text_view"
        android:layout_marginTop="1dip"
        android:layout_toEndOf="@+id/summary_thumbnail_ll"
        android:text="Lily"
        android:textColor="#09b900"
        android:textSize="15sp"
        android:textStyle="bold" />

    <!-- Correct answer -->
    <TextView
        android:id="@+id/summary_answer_text_view"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/summary_response_text_view"
        android:layout_marginTop="1dip"
        android:layout_toEndOf="@+id/summary_thumbnail_ll"
        android:text="Poppy"
        android:textColor="#09b900"
        android:textSize="15sp"
        android:textStyle="bold" />

</RelativeLayout>